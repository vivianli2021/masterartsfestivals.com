{
  "name": "masterartsfestivals-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "node ./dist/server/index.js",
    "lint": "eslint . || true",
    "format": "prettier --write . || true",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {
    "astro": "latest"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0"
  }
}
